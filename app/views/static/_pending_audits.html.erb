<h2>Pending your Confirmation</h2>
<% @pending_audit_confirmations.each do |audit| %>
  <%= link_to "I confirm I did not perform any overtime for, week starting: #{audit.start_date}", confirm_audit_log_path(audit), 
      class: "btn btn-primary mt-1", id: "audit-log-confirm#{audit.id}", data: { confirm: "Are you sure you want to confirm you didn't complete any overtime?" } %>
<% end %>